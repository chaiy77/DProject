import { Button } from '@material-ui/core';
import styled from '@emotion/styled';

export default styled(Button)`
  background: #3abbc9;
  border-radius: 3px;
  border: 0;
  color: white;
  height: 3em;
  margin: 1em 0.5em;
  padding: 0 3em;
  box-shadow: 0 1px 4px 1px rgba(255, 105, 135, 0.3);
`;
